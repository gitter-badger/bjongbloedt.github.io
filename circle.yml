machine:
  node:
    version: v6.1.0
general:
  branches:
    only:
      - src
deployment:
  production:
    branch: src
    commands:
    - git config --global user.email "bjongbloedt@hotmail.com"
    - git config --global user.name "bjongbloedt"
    - npm run deploy
